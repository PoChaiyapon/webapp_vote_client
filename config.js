// Production configuration
window.APP_CONFIG = {
    API_URL: 'http://localhost:3000/api',  // Will be relative to the current domain
    SOCKET_URL: 'http://localhost:3000'    // Empty string will connect to current host
};
