// Production configuration
window.APP_CONFIG = {
    API_URL: 'https://webapp-vote-server.onrender.com/api',  // Will be relative to the current domain
    SOCKET_URL: 'https://webapp-vote-server.onrender.com'    // Empty string will connect to current host
};
